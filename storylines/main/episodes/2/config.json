{
    /* 章节的基础信息 */
  "storyFile": "02.js",//故事主体的位置
  "episodeName": "My Name is James Bunder",//故事名称
  //"version": "0.0.1",//主版本.子版本.补丁(DEPRECATED 由故事线配置中的version代替)
  "episodeID": "helpmeout.mainepisodes.2",//故事唯一ID
  //"debug": true,  //调试模式(DEPRECATED)
  "settings": {
    /* 有关章节的配置 */
    "OS_limit": {
      //对系统类型进行筛选，include与exclude只能选择其一使用，不填默认include_all
      //在exclude设定的系统将无法加载该剧情，其余则可加载剧情
      //BLM, we are not going to use 'blacklist' and 'whitelist', 'include' & 'exclude' as alternative
      /*"include": [
        "all"
      ],
      "exclude": [
      ]*/
    },
    "OS_branch": {
      //对系统类型进行区分，以不同的方式启动剧情文件，不进行区分或未在列表将以main方式启动
      "linux": "ln",
      "mac": "osx",
      "ios": "i",
      "windows": "wd"
    },
    "language_support": [
      "en_US"/*,
      "en_UK",
      "zh_CN",
      "zh_HK",
      "zh_MA",
      "zh_TW"*/
    ],
    "require_language_consistancy": true,
    "specifyLanguagePath": []
  }
}
